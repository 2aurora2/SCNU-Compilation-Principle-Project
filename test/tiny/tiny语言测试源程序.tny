read x;{comment}
if x > 0 then
  fact := 1 + 2 / 2 % 2 ^ 1 - 1;
  repeat
    fact := fact * x;
    x := x - 1
  until x <= 0;
  write fact
else
  if x = 0 then
    write x
  end;
  if x > 0 then
    write x + 1
  end
end;
repeat
  x := x + (1 + 2)
until x >= 10
